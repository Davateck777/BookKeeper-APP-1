/* TYPOGRAPHY: Keeps headings, labels, and body text consistent across screens. */
package com.bookkeeper.app.core.theme

import androidx.compose.material3.Typography

val BookKeeperTypography = Typography()
